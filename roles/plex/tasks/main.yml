- name: Make sure Plex is running
  ansible.builtin.systemd:
    state: started
    name: plexmediaserver
- name: Make sure NFS is mounted
  ansible.posix.mount:
    fstype: nfs
    src: nfs.emwj.lan:/home/emerson/ext/Plex
    path: /home/emerson/nfs
    opts: defaults
    state: mounted
